spring:
  datasource:
    url: jdbc:mysql://192.168.1.9:3306/mathdb?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: passw0rd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
#    hibernate:
#      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8080 